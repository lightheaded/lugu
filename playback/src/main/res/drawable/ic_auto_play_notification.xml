<!--
  The small icon for the notification shown while a book is about to start by itself.

  A notification small icon is drawn as a silhouette by the system — only the alpha channel
  survives — so this is a single opaque path and no colour worth naming. It exists rather
  than borrowing Media3's because the icon is ours to keep stable: the notification it sits
  on is replaced by the player's a few seconds later, and the two should not visibly differ.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FFFFFFFF">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M8,5.14V19.14L19,12.14L8,5.14Z" />
</vector>
